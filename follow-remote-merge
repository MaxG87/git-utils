#!/usr/bin/env bash

set -eu

function print-usage() {
    cat <<EOF
$0: [OPTION]... [target-branch]

Follow a merge that was performed on a remote platform, e.g. GitHub or GitLab.
If HEAD is contained in the target branch, HEAD is switched to the target
branch and the source branch is deleted.

The target branch defaults to \`develop\`, \`main\`, or \`master\`, in that
order.

OPTIONS:

  -C DIRECTORY Execute git commands in DIRECTORY. Defaults to PWD.
  -f, --force  Skip checks when moving to target branch.
  -h, --help   Print this message and exit.

Examples:
follow-remote-merge
follow-remote-merge develop
follow-remote-merge -C ~/repos/some-repo -f weird-default-branch
EOF
}

function die() {
    local msg=$1
    local code=${2-1} # default exit status 1
    echo "$msg" >&2
    exit "$code"
}

function main() {

    local workingDirectory=.
    local targetBranch

    while [[ $# -gt 0 ]]; do
        case "$1" in
            -h | --help)
                print-usage
                exit 0
                ;;
            -C)
                shift
                workingDirectory="$1"
                ;;
            -f | --force)
                die "Force intended but not yet supported!"
                ;;
            *)
                targetBranch="$1"
                ;;
        esac
        shift
    done

    test -d "$workingDirectory" ||
        die "Working directory $workingDirectory does not exist!"
    GIT_CMD="git -C $workingDirectory"

    if [[ ! -v targetBranch ]]; then
        targetBranch=$(get-default-target-branch)
        echo "No target branch specified, using '${targetBranch}'."
    fi
    branch-exists "$targetBranch" ||
        die "Target branch $targetBranch does not exist!"

    follow-remote-merge "$targetBranch"
}

function branch-exists() {
    $GIT_CMD show-ref --quiet refs/heads/"$1"
}

function get-default-target-branch() {
    for branch in develop main master; do
        if branch-exists $branch; then
            echo $branch
            return
        fi
    done
    echo "None of the default branches exist!" >&2
    exit 1
}

function follow-remote-merge() {
    local target="$1"
    local source upstream_remote upstream_branch
    source=$($GIT_CMD branch --show-current)
    upstream_remote=$(get-remote-of "$source")
    upstream_branch=$(get-upstream-of "$source")

    update-local-target-branch "$target"

    $GIT_CMD diff --quiet ||
        die "Found uncommitted changes in ${source}."

    if is-ancestor-to "$target" || has-no-diff-commit "$target"; then
        $GIT_CMD switch "${target}"
        $GIT_CMD branch -D "${source}" >/dev/null ||
            die "Could not delete local branch ${source}."
        if upstream-branch-exists "$upstream_remote" "$upstream_branch"; then
            remove-upstream-branch "$upstream_remote" "$upstream_branch"
        fi
    else
        die "Found unmerged changes in ${source}."
    fi
}

function get-remote-of() {
    local branch="$1"
    $GIT_CMD config --get "branch.${branch}.remote"
}

function get-upstream-of() {
    local branch="$1"
    $GIT_CMD config --get "branch.${branch}.merge" | sed 's|refs/heads/||'
}

function update-local-target-branch() {
    local target="$1"
    local remote targetUpstream
    remote=$(get-remote-of "$target")
    targetUpstream=$(get-upstream-of "$target")
    git fetch "$remote" >/dev/null ||
        die "Could not fetch from remote ${remote}."
    fullRemoteBranch="${remote}/${targetUpstream}"

    localTargetRev=$($GIT_CMD rev-parse "$target")
    remoteTargetRev=$($GIT_CMD rev-parse "${fullRemoteBranch}")
    if git merge-base --is-ancestor "${fullRemoteBranch}" "$target" &&
        [[ "${localTargetRev}" != "${remoteTargetRev}" ]]; then
        die "Local target branch ${target} is ahead of ${fullRemoteBranch}. Please resolve manually!"
    elif ! git merge-base --is-ancestor "$target" "${fullRemoteBranch}"; then
        die "Local target branch ${target} cannot be fast-forwarded to ${fullRemoteBranch}."
    fi
    $GIT_CMD fetch "$remote" "$targetUpstream":"$target" >/dev/null ||
        die "Could not update local target branch ${target}."

}

function has-no-diff-commit() {
    local target="$1"
    local mergeBase

    if is-ancestor-to "$target"; then
        # efficient case first
        return 0
    fi

    # Iterate over all commits between mergeBase and the target branch and
    # check if to one of these the diff is empty. This allows to detect squash
    # merges at any point in the history.
    mergeBase="$($GIT_CMD merge-base HEAD "$target")"
    $GIT_CMD log "$mergeBase".."$target" --oneline |
        cut -f1 -d ' ' |
        xargs -I% -n1 bash -c "$GIT_CMD diff --quiet %; echo \$?" |
        grep -q 0
}

function is-ancestor-to() {
    local target="$1"
    $GIT_CMD merge-base --is-ancestor HEAD "$target"
}

function upstream-branch-exists() {
    local remote="$1"
    local upstream_branch="$2"
    $GIT_CMD ls-remote --exit-code --heads "$remote" "${upstream_branch}" >/dev/null
}

function remove-upstream-branch() {
    local remote="$1"
    local upstream_branch="$2"
    $GIT_CMD push "${remote}" --delete "${upstream_branch}" &>/dev/null ||
        die "Could not delete remote branch ${upstream_branch} on ${remote}."
}

main "$@"
